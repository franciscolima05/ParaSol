# Backend

Servicios off-chain (opcional). Usar solo si el caso de uso lo requiere: indexación custom, integraciones con APIs externas, lógica que no debería ser on-chain, autenticación con sesiones, etc.

## Stack sugerido

- Node.js + Fastify (o Express)
- TypeScript
- Prisma / Drizzle si hay base de datos
